# ModelScope deployment 1.0.12

选择托管 MCP、STDIO。command �?`uvx`，args 只有 `crc-lnm-medical-agent@1.0.12`。不要添�?URL、host、port、transport 或环境补丁�?
部署后依次验�?initialize、tools/list 精确六项、`crc_lnm_get_model_info` 返回 seed_2024/member_count 1，再�?demo_case_001 完成六工具链路。确�?stderr 可含诊断日志�?stdout �?MCP 帧，进程退出后无残留�?
上传/发布动作由用户在审阅 wheel、checksum、依赖和未验证风险后手工完成�?